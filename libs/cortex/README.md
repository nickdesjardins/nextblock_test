# cortex

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cortex` to build the library.

## Running unit tests

Run `nx test cortex` to execute the unit tests via [Vitest](https://vitest.dev/).
